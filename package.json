{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:back": "cd backend && nest build",
    "build:front": "cd frontend && tsc && vite build",
    "dev": "cd frontend && vite",
    "server": "cd backend && nest start --watch"
  }
}
